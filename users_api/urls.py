# C:\Users\nexon\hyundaiautoever_sideproject\users_api\urls.py

from django.urls import path
# from .views import UserRegistrationView # 나중에 뷰를 추가할 때 사용

urlpatterns = [
    # 여기에 users_api 앱의 URL 패턴들을 정의합니다.
    # 예: path('register/', UserRegistrationView.as_view(), name='user-register'),
    # 현재는 비어있어도 무방합니다.
]